---
import MomentLogo from '../../public/logo/monochrome.svg';
---

<header class="siteName">
	<img class="logo" src={MomentLogo} alt="The Moment" />
</header>

<style lang="scss">
.siteName {
	display: grid;
	grid: auto / 48px 1fr;
	column-gap: 12px;
	align-items: center;
	padding: 16px 16px 0;

	@media screen and (min-width: 768px) {
		grid: auto /
					auto;
		row-gap: 12px;
	}
}

.logo {
	width: 48px;
	aspect-ratio: 1;
	fill: currentColor;
}

.fullname {
	margin: 0;
	color: hsl(var(--color-midnight1100));
	font-size: clamp(1.6rem, 1.8vw, 2rem);
	font-weight: 400;
}
</style>
